"""OPLM command-line scripts.

A package (not the installed ``oplm`` CLI) so each tool runs via
``python -m scripts.<name>`` from the repo root. Holds the μP tooling:
:mod:`scripts.mup_coord_check` (the coordinate-check correctness gate) and the
Phase-7 LR-sweep harness.
"""

from __future__ import annotations
