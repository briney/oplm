The nodes table is fine, but to be sure that everything fits into memory, we need "full" gradient checkpointing. for the time limits (confirm included, at 48h) I'd be very generous with the limit -- max is 1 week, so we definitely don't want to get into a situation where the run gets cut off most of the way through because we tried to get too cute. I feel like the scale sub-block might actually be better suited in its own separate config -- this repo is designed to be completely open (not just open weights for inference), so that people can recreate every step of the training process. Putting the scale sub-block under sweep might make sense to us, but could be confusing to someone looking at the codebase for the first time (and I can imagine someone might want to leverage the scale tooling for things other than just a uP lr sweep.