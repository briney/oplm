# ~800M parameters
# 32 width:depth ratio
model:
  hidden_size: 1280
  num_hidden_layers: 40
  num_attention_heads: 20
  # gradient_checkpointing: true
  # gradient_checkpointing_mode: selective # full | selective
